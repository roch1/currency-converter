version: 2.1 # use CircleCI 2.1
jobs:
  build:

    working_directory: ~/currency-converter-circleci-build # directory where steps will run

    docker: # run the steps with Docker
      - image: circleci/openjdk:11.0.2-jdk # build steps will run in this container

    steps: # a collection of executable commands

      - checkout # check out source code to working directory

      - restore_cache: # restore the saved cache after the first run or if `pom.xml` has changed
          # Read about caching dependencies: https://circleci.com/docs/2.0/caching/
          key: currency-converter-circleci-{{ checksum "pom.xml" }}

      - run: mvn dependency:go-offline # install all project dependencies locally so project offline can be built offline

      - save_cache: # saves the project dependencies
          paths:
            - ~/.m2
          key: currency-converter-circleci-{{ checksum "pom.xml" }}

      - run: mvn clean package # run all maven lifecycle phases until the phase specified (remove files generated by previous build, compile source, run tests, package compiled code into distributable format)

      - store_test_results: # uploads the test metadata from the `target/surefire-reports` directory so that it can show up in the CircleCI dashboard. 
          # Upload test results for display in Test Summary: https://circleci.com/docs/2.0/collect-test-data/
          path: target/surefire-reports

      - store_artifacts: # store the uberjar as an artifact
          # Upload test summary for display in Artifacts: https://circleci.com/docs/2.0/artifacts/
          path: target/currency-conversion-calculator-1.0-SNAPSHOT.jar
      # See https://circleci.com/docs/2.0/deployment-integrations/ for deploy examples    